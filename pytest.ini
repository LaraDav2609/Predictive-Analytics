[pytest]
testpaths = tests
markers =
    slow: tests that take significant time (e.g., pymc sampling)
    network: tests that require internet access (e.g., FastF1 API)
# By default skip slow + network tests; opt in via `pytest -m slow` or `-m network` or `-m ""`.
addopts = -m "not slow and not network"
