"""f1_ml — Formula 1 race prediction and trading ML stack.

Public API surface is intentionally thin; consumers should import from sub-packages
(features, ratings, events, core, sequence, strategy, simulator, markets, backtest).
"""

__version__ = "0.1.0"
